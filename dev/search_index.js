var documenterSearchIndex = {"docs":
[{"category":"section","location":"api/#API-reference","page":"API reference","text":"","title":"API reference"},{"category":"section","location":"api/#Markov-processes","page":"API reference","text":"","title":"Markov processes"},{"category":"section","location":"api/#State-space-interface","page":"API reference","text":"Every process implements the same small shape interface:","title":"State-space interface"},{"category":"section","location":"api/#The-generator-and-its-operators","page":"API reference","text":"","title":"The generator and its operators"},{"category":"section","location":"api/#Additive-functionals-and-tail-indices","page":"API reference","text":"","title":"Additive functionals and tail indices"},{"category":"section","location":"api/#Finite-differences","page":"API reference","text":"","title":"Finite differences"},{"category":"type","location":"api/#InfinitesimalGenerators.ContinuousTimeMarkovProcess","page":"API reference","text":"ContinuousTimeMarkovProcess{N}\n\nAbstract supertype for continuous-time Markov processes with N tensor-product state-space axes.\n\nConcrete subtypes must define state_space(X) and generator(X). state_space(X) must return a tuple of state-space axes, including for one-dimensional processes. size(X) is derived from those axes, and ndims(X) returns the type parameter N.\n\n\n\n\n\n","title":"InfinitesimalGenerators.ContinuousTimeMarkovProcess"},{"category":"type","location":"api/#InfinitesimalGenerators.ContinuousTimeMarkovChain","page":"API reference","text":"ContinuousTimeMarkovChain(states, Q)\nContinuousTimeMarkovChain(Q)\n\nReturns a finite-state continuous-time Markov chain on the discrete state space states, with generator (transition-rate) matrix Q.\n\nThe matrix Q is a generator: rows must sum to zero and off-diagonal entries must be nonnegative transition rates. It is not a discrete-time transition probability matrix.\n\n\n\n\n\n","title":"InfinitesimalGenerators.ContinuousTimeMarkovChain"},{"category":"type","location":"api/#InfinitesimalGenerators.DiffusionProcess","page":"API reference","text":"DiffusionProcess(x, μx, σx)\n\nReturns the diffusion process\n\ndx_t = μ(x_t) dt + σ(x_t) dZ_t\n\ndiscretized on the strictly increasing grid x (possibly non-uniform), where μx and σx are the drift and volatility evaluated on the grid. The drift is discretized by upwinding and the boundaries are reflecting, so generator always returns a valid generator (transition-rate) matrix.\n\n\n\n\n\n","title":"InfinitesimalGenerators.DiffusionProcess"},{"category":"function","location":"api/#InfinitesimalGenerators.OrnsteinUhlenbeck","page":"API reference","text":"OrnsteinUhlenbeck(; xbar = 0.0, κ = 0.1, σ = 1.0, p = 1e-10, length = 100, xmin, xmax, pow = 1)\n\nReturns the Ornstein–Uhlenbeck process\n\ndx_t = -κ * (x_t - xbar) * dt + σ * dZ_t\n\ndiscretized as a DiffusionProcess on an automatically chosen grid.\n\nBy default the grid has length points spanning the p and 1 - p quantiles of the stationary distribution N(xbar, σ^2 / 2κ); pass xmin and xmax to override the limits. pow controls the grid spacing (when xmin > 0, points are uniform in x^(1/pow), so pow > 1 concentrates points near xmin). The default p = 1e-10 is deliberately extreme: reflecting boundaries distort solutions near the edges of the grid, and a wide grid pushes that distortion where the process never goes — which matters especially for tail_index.\n\n\n\n\n\n","title":"InfinitesimalGenerators.OrnsteinUhlenbeck"},{"category":"function","location":"api/#InfinitesimalGenerators.CoxIngersollRoss","page":"API reference","text":"CoxIngersollRoss(; xbar = 0.1, κ = 0.1, σ = 1.0, p = 1e-10, length = 100, xmin, xmax, pow = 2)\n\nReturns the Cox–Ingersoll–Ross process\n\ndx_t = -κ * (x_t - xbar) * dt + σ * sqrt(x_t) * dZ_t\n\ndiscretized as a DiffusionProcess on an automatically chosen grid. Requires the Feller condition 2κ * xbar / σ^2 > 1, so that 0 is not attainable.\n\nBy default the grid has length points spanning the p and 1 - p quantiles of the stationary Gamma distribution; pass xmin and xmax to override the limits. pow controls the grid spacing (points are uniform in x^(1/pow), so the default pow = 2 concentrates points near zero, where the volatility is smallest).\n\n\n\n\n\n","title":"InfinitesimalGenerators.CoxIngersollRoss"},{"category":"type","location":"api/#InfinitesimalGenerators.MultivariateDiffusionProcess","page":"API reference","text":"MultivariateDiffusionProcess(grid; drift, variance, covariance = nothing)\n\nReturns a diffusion process on a tensor-product grid.\n\ngrid is a NamedTuple mapping state names to strictly increasing grid vectors. drift has the same names as grid; each entry is either a scalar or an array with the same size as the tensor grid. variance has the same names and gives the diagonal entries of the instantaneous covariance matrix. Cross terms are optional and are passed as pair names such as (; xy = axy).\n\n\n\n\n\n","title":"InfinitesimalGenerators.MultivariateDiffusionProcess"},{"category":"type","location":"api/#InfinitesimalGenerators.ProductProcess","page":"API reference","text":"ProductProcess(X, Y, ...)\n\nReturns the independent product of Markov processes in argument order.\n\n\n\n\n\n","title":"InfinitesimalGenerators.ProductProcess"},{"category":"type","location":"api/#InfinitesimalGenerators.SwitchingProcess","page":"API reference","text":"SwitchingProcess(Z, Xs)\n\nReturns a Markov process whose dynamics are modulated by the autonomous Markov process Z — any process in the package, e.g. a ContinuousTimeMarkovChain (regime switching) or a DiffusionProcess (continuous modulation). In the i-th state of Z (in column-major order for multivariate Z), the process follows Xs[i]; all Xs must share the same state space.\n\n\n\n\n\n","title":"InfinitesimalGenerators.SwitchingProcess"},{"category":"function","location":"api/#InfinitesimalGenerators.state_space","page":"API reference","text":"state_space(X::ContinuousTimeMarkovProcess)\n\nReturn the state-space axes of X as a tuple.\n\nThe return value is always a tuple, including for one-dimensional processes: state_space(X)[1] is the first axis, and only(state_space(X)) is the grid of a one-dimensional process.\n\n\n\n\n\n","title":"InfinitesimalGenerators.state_space"},{"category":"method","location":"api/#Base.size-Tuple{ContinuousTimeMarkovProcess}","page":"API reference","text":"size(X::ContinuousTimeMarkovProcess)\n\nReturn the tensor shape of arrays defined on the state space of X.\n\nThis is the length of each state-space axis. Operators such as stationary_distribution(X) and feynman_kac(X, ...) use this shape for process-level inputs and outputs.\n\n\n\n\n\n","title":"Base.size"},{"category":"method","location":"api/#Base.length-Tuple{ContinuousTimeMarkovProcess}","page":"API reference","text":"length(X::ContinuousTimeMarkovProcess)\n\nReturn the total number of grid points or finite states in X, equal to prod(size(X)). This is the length of flattened state vectors and the row/column dimension of generator(X).\n\n\n\n\n\n","title":"Base.length"},{"category":"method","location":"api/#Base.ndims-Tuple{ContinuousTimeMarkovProcess}","page":"API reference","text":"ndims(X::ContinuousTimeMarkovProcess)\n\nReturn the number of tensor-product state-space axes of X.\n\n\n\n\n\n","title":"Base.ndims"},{"category":"function","location":"api/#InfinitesimalGenerators.generator","page":"API reference","text":"generator(X::ContinuousTimeMarkovProcess)\n\nReturn the generator (transition-rate) matrix 𝔸 of the discretized process — the matrix representation of the infinitesimal generator\n\n𝔸: f ↦ lim_{t→0} (E[f(x_t) | x_0 = x] - f(x)) / t\n\nacting on the state space flattened in column-major order. Rows sum to zero and off-diagonal entries are non-negative transition rates.\n\n\n\n\n\n","title":"InfinitesimalGenerators.generator"},{"category":"function","location":"api/#InfinitesimalGenerators.check_generator","page":"API reference","text":"check_generator(𝔸::AbstractMatrix; atol = nothing)\n\nCheck that 𝔸 is a valid generator (transition-rate) matrix: square, with nonnegative off-diagonal entries and rows summing to zero — that is, a Metzler matrix whose rows sum to zero.\n\nA violation beyond atol emits a warning reporting its size rather than throwing — a matrix that is close to a generator often still yields accurate results. Non-square or empty matrices throw an ArgumentError. Returns 𝔸, so the call can be used inline.\n\natol defaults to sqrt(eps) * max(1, maximum(abs, diag(𝔸))) (for a valid generator, the largest entry in absolute value is on the diagonal). The off-diagonal check is minimum(𝔸 - Diagonal(diag(𝔸))), which preserves the structure of the matrix — sparse matrices are checked in one pass over their stored entries, tridiagonal ones over their bands.\n\nNote that a tilted generator (tilted_generator(m, ξ)) is intentionally not a generator in this sense: it has nonnegative off-diagonal entries but nonzero row sums — that is why E[e^{ξ mₜ}] grows at rate Λ(ξ) instead of remaining a probability.\n\n\n\n\n\n","title":"InfinitesimalGenerators.check_generator"},{"category":"function","location":"api/#InfinitesimalGenerators.stationary_distribution","page":"API reference","text":"stationary_distribution(𝔸::AbstractMatrix; δ = 0.0, rebirth = Ones(size(𝔸, 1)))\n\nCompute the stationary distribution corresponding to the generator matrix 𝔸 — the solution of the Kolmogorov forward equation 𝔸'g = 0, normalized to sum to one.\n\nThe result is a vector of probability masses, one per state — sum(g) == 1 with no grid weights — so expectations are unweighted dot products sum(g .* f). To get a density for a discretized diffusion, divide the masses by the cell widths of the grid.\n\nWith a death rate δ > 0 and a rebirth distribution ψ given by rebirth, returns instead the resolvent (δI - 𝔸')⁻¹ δψ — the stationary distribution when units die at rate δ and are reborn with distribution ψ.\n\n\n\n\n\nstationary_distribution(X::ContinuousTimeMarkovProcess; δ = 0.0, rebirth = Ones(length(X)), kwargs...)\n\nCompute the stationary distribution of the Markov process X, returned as an array of probability masses shaped like size(X): the array sums to one with no grid weights, and expectations are unweighted dot products sum(g .* f). To get a density for a diffusion, divide by the cell widths of the grid — essential when the grid is non-uniform.\n\nThe keywords δ and rebirth have the same meaning as in the matrix form; rebirth is an array shaped like size(X). Remaining keyword arguments are forwarded to generator(X) — e.g. check = :warn for a MultivariateDiffusionProcess.\n\n\n\n\n\n","title":"InfinitesimalGenerators.stationary_distribution"},{"category":"function","location":"api/#InfinitesimalGenerators.feynman_kac","page":"API reference","text":"feynman_kac(𝔸::AbstractMatrix, ts; f, ψ, v, direction = :backward)\n\nSolve the Feynman–Kac PDE associated with a generator matrix 𝔸 on the increasing time grid ts, using implicit Euler time steps.\n\nWith direction = :backward, returns the solution of\n\nu(x, ts[end]) = ψ(x)\n0 = uₜ + 𝔸u - v(x, t)u + f(x, t)\n\nor, equivalently, in integral form,\n\nu(x, t) = E[∫ₜᵀ e^{-∫ₜˢ v(x_u) du} f(x_s) ds + e^{-∫ₜᵀ v(x_u) du} ψ(x_T) | xₜ = x].\n\nWith direction = :forward, returns the solution of\n\nu(x, ts[1]) = ψ(x)\nuₜ = 𝔸u - v(x, t)u + f(x, t)\n\nor, equivalently, in integral form,\n\nu(x, t) = E[∫₀ᵗ e^{-∫₀ˢ v(x_u) du} f(x_s) ds + e^{-∫₀ᵗ v(x_u) du} ψ(xₜ) | x₀ = x].\n\nThe inputs f, ψ, and v are vectors of length size(𝔸, 1). The inputs f and v may also be matrices with length(ts) columns (one per date) or with a single column (held constant over time).\n\n\n\n\n\nfeynman_kac(X::ContinuousTimeMarkovProcess, ts; f = nothing, ψ = nothing, v = nothing, direction = :backward, kwargs...)\n\nSolve the Feynman–Kac PDE associated with a Markov process X on the time grid ts, using implicit Euler time steps.\n\nFor the process form, f, ψ, and v are arrays shaped like the state space (size(X)); f and v may also carry a trailing time dimension of length length(ts). The result has one slice per date in ts. Remaining keyword arguments are forwarded to generator(X) — e.g. check = :warn for a MultivariateDiffusionProcess.\n\n\n\n\n\n","title":"InfinitesimalGenerators.feynman_kac"},{"category":"function","location":"api/#InfinitesimalGenerators.jointoperator","page":"API reference","text":"jointoperator(operators, Q)\n\nCombine one generator matrix per regime with a regime generator (transition-rate) matrix Q into the block generator of the joint process. Most users can use SwitchingProcess instead.\n\n\n\n\n\n","title":"InfinitesimalGenerators.jointoperator"},{"category":"function","location":"api/#InfinitesimalGenerators.principal_eigenvalue","page":"API reference","text":"principal_eigenvalue(𝔸; r0 = ones(size(𝔸, 1)), η0 = nothing, maxiter = 100, tol = 1e-12)\n\nCompute the principal eigenvalue and eigenvector of a Metzler matrix 𝔸 (i.e. a matrix with non-negative off-diagonal entries), returned as a tuple (η, r).\n\nBy Perron-Frobenius, the eigenvalue η with largest real part is real, and the corresponding eigenvector r is strictly positive. Metzlerity is what makes this hold — generators and tilted generators both satisfy it — so the matrix is checked on entry, with a warning if an off-diagonal entry is negative (results may still be accurate when the violation is small).\n\nTwo cases:\n\nIf rows or columns sum to zero (𝔸 is a generator, or its transpose), then η = 0. The eigenvector is found by solving 𝔸r = 0 with r[1] = 1; the row-sum test is scaled by the size of the diagonal, so it is robust to the rounding of generator assembly. If that solve is singular, the generator is reducible (it has states that do not communicate), the stationary eigenvector is not unique, and an ArgumentError says so.\nOtherwise, η is found by inverse iteration with Rayleigh quotient updates. At each step, we solve (𝔸 - σI)w = r, normalize w, and update the shift σ via the Rayleigh quotient σ = r'𝔸r. This converges cubically to a nearby eigenvalue. The initial shift is the max row sum — a Gershgorin upper bound on the real part of the spectrum. When 𝔸 has a real spectrum (e.g. the tridiagonal generators of 1-D diffusions, which are similar to symmetric matrices), the eigenvalue nearest that shift is the principal one, so the iteration lands on η. For a general Metzler matrix with complex eigenvalues this is not guaranteed; pass η0 to start from a known bound if needed. If a shift lands exactly on an eigenvalue (e.g. the Gershgorin bound when all row sums are equal, so the bound is attained), it is nudged by tol to keep the solve nonsingular; the Rayleigh quotient then recovers the exact eigenvalue. For tridiagonal 𝔸, each iteration costs O(n).\n\n\n\n\n\n","title":"InfinitesimalGenerators.principal_eigenvalue"},{"category":"type","location":"api/#InfinitesimalGenerators.AdditiveFunctional","page":"API reference","text":"AdditiveFunctional\n\nAbstract supertype for additive functionals m of a Markov process — cumulative quantities whose increments depend on the state, such as cumulative log growth. The defining verb is tilted_generator(m, ξ); the operators cgf, tail_index, and principal_eigenvalue are computed from it.\n\n\n\n\n\n","title":"InfinitesimalGenerators.AdditiveFunctional"},{"category":"function","location":"api/#InfinitesimalGenerators.tilted_generator","page":"API reference","text":"tilted_generator(m::AdditiveFunctional, ξ)\n\nReturn the tilted generator matrix 𝔸_ξ of the additive functional m, i.e. the matrix representation of the operator\n\n𝔸_ξ: f ↦ lim_{t→0} (E[e^{ξ mₜ} f(xₜ) | x₀ = x] - f(x)) / t.\n\ntilted_generator(m, 0) is the generator of the underlying process. Everything the package computes about additive functionals derives from this matrix: cgf and tail_index use its principal eigenvalue, and it can be passed directly to feynman_kac to compute finite-horizon moments E[e^{ξ mₜ} ψ(xₜ)]. To define a custom additive functional, subtype AdditiveFunctional and define this function.\n\nA tilted generator is not a generator in the sense of check_generator: its off-diagonal entries must be nonnegative (a Metzler matrix — equivalently, a generator plus a diagonal), but its rows need not sum to zero. Metzlerity is what makes the principal eigenvalue real and its eigenvector positive; a custom implementation must preserve it (principal_eigenvalue checks it on entry and warns if it fails).\n\n\n\n\n\n","title":"InfinitesimalGenerators.tilted_generator"},{"category":"function","location":"api/#InfinitesimalGenerators.cgf","page":"API reference","text":"cgf(m::AdditiveFunctional, ξ; r0 = nothing, η0 = nothing)\n\nReturn the long-run scaled cumulant generating function of m evaluated at ξ,\n\nΛ(ξ) = lim_{t→∞} log(E[e^{ξ mₜ}]) / t,\n\ncomputed as the principal eigenvalue of the tilted generator tilted_generator(m, ξ) by inverse iteration (Hansen and Scheinkman 2009). r0 is an initial guess for the eigenvector (defaults to a vector of ones) and η0 an initial guess for the eigenvalue. Use cgf_eigenvector to also obtain the associated eigenvector.\n\n\n\n\n\n","title":"InfinitesimalGenerators.cgf"},{"category":"function","location":"api/#InfinitesimalGenerators.cgf_eigenvector","page":"API reference","text":"cgf_eigenvector(m::AdditiveFunctional, ξ, side = :right; r0 = nothing, η0 = nothing)\n\nReturn the pair (Λ(ξ), vector): the long-run scaled cumulant generating function of m at ξ (as in cgf) together with the associated principal eigenvector of the tilted generator.\n\nWith side = :right, the right eigenvector — the Hansen–Scheinkman eigenfunction. With side = :left, the left eigenvector, normalized to sum to one — the stationary distribution of the twisted process, i.e. the distribution over states from which the long-run growth of E[e^{ξ mₜ}] is achieved.\n\n\n\n\n\n","title":"InfinitesimalGenerators.cgf_eigenvector"},{"category":"function","location":"api/#InfinitesimalGenerators.tail_index","page":"API reference","text":"tail_index(m::AdditiveFunctional; δ = 0, bracket = (1e-5, 1e3), xatol = 1e-4)\n\nCompute the tail index of the stationary distribution of e^m when units die (are reset) at rate δ, i.e. the ζ such that cgf(m, ζ) = δ.\n\nThe root is searched for in bracket; if cgf(m, ξ) - δ has the same sign at both ends, an ArgumentError reports the two values so the bracket can be moved. Remaining keyword arguments are passed to Roots.fzero.\n\n\n\n\n\ntail_index(μ::Number, σ::Number; δ = 0)\n\nClosed form for constant coefficients: the tail index of the stationary distribution of a size w growing as dw/w = μ dt + σ dZ (note that μ is the arithmetic growth rate of w itself, equal to the drift of log w plus σ^2/2) with death rate δ.\n\n\n\n\n\n","title":"InfinitesimalGenerators.tail_index"},{"category":"type","location":"api/#InfinitesimalGenerators.AdditiveFunctionalDiffusion","page":"API reference","text":"AdditiveFunctionalDiffusion(X::DiffusionProcess, μm, σm; ρ = 0.0)\n\nAn additive functional m of the diffusion X, defined by\n\ndmₜ = μm(xₜ) dt + σm(xₜ) dZᵐₜ,        corr(dZᵐₜ, dZₜ) = ρ,\n\nwhere μm and σm are vectors evaluated on the grid of X.\n\nLegacy type: AdditiveFunctional(X, μm, σm; ρ = ρ) constructs an equivalent functional, and is the preferred entry point; you rarely need to construct this type directly.\n\n\n\n\n\n","title":"InfinitesimalGenerators.AdditiveFunctionalDiffusion"},{"category":"type","location":"api/#InfinitesimalGenerators.FirstDerivative","page":"API reference","text":"FirstDerivative(x, f; direction = :forward, bc = (0, 0))\nFirstDerivative(grid, F, dim; direction = :forward, bc = (0, 0))\n\nLazily compute first-order derivatives (using finite-difference scheme) on a grid. The result is an AbstractArray whose entries are computed on demand; use collect to materialize it.\n\nIn one dimension, x is a grid vector and f is a vector with length(f) == length(x). In multiple dimensions, grid is a tuple of axis vectors such as (xs, ys), F is an array with size (length(xs), length(ys)), and dim is the dimension number to differentiate. NamedTuple grids with symbolic dimensions are also accepted as a convenience.\n\ndirection selects the one-sided difference: :forward (synonyms :up, :upward) or :backward (synonyms :down, :downward).\n\nbc is the value of the first derivative at the lower and upper boundaries.\n\n\n\n\n\n","title":"InfinitesimalGenerators.FirstDerivative"},{"category":"type","location":"api/#InfinitesimalGenerators.SecondDerivative","page":"API reference","text":"SecondDerivative(x, f; bc = (0, 0))\nSecondDerivative(grid, F, dim; bc = (0, 0))\nSecondDerivative(grid, F, dim1, dim2; direction = :up)\n\nLazily compute second-order derivatives (using finite-difference scheme) on a grid. The result is an AbstractArray whose entries are computed on demand; use collect to materialize it.\n\nIn one dimension, x is a grid vector and f is a vector with length(f) == length(x). In multiple dimensions, grid is a tuple of axis vectors such as (xs, ys), and F is an array with size (length(xs), length(ys)). NamedTuple grids with symbolic dimensions are also accepted as a convenience.\n\nUse SecondDerivative(grid, F, 1, 1) for own second derivatives and SecondDerivative(grid, F, 1, 2; direction = :up) for directional cross derivatives. The :up direction (synonyms :forward, :upward) is the main-diagonal stencil; :down (synonyms :backward, :downward) is the anti-diagonal stencil.\n\nFor own second derivatives, bc is the value of the first derivative at the lower and upper boundaries. Cross derivatives do not accept boundary conditions (the stencil is clamped at the edges of the grid) and throw if a nonzero bc is passed.\n\n\n\n\n\n","title":"InfinitesimalGenerators.SecondDerivative"},{"category":"section","location":"univariate/#Univariate-processes","page":"Univariate processes","text":"Every process in the package implements the same three verbs — generator(X) (the generator (transition-rate) matrix of the discretized process), state_space(X) (a tuple of state-space axes), and size(X) (the tensor shape of the state space) — and everything else (stationary distributions, expectations, tail indices) is built on them. Univariate processes subtype ContinuousTimeMarkovProcess{1}, so ndims(X) == 1 and state_space(X) and size(X) are one-element tuples. This page tours the univariate process types.","title":"Univariate processes"},{"category":"section","location":"univariate/#Finite-state-continuous-time-Markov-chains","page":"Univariate processes","text":"The simplest process is one whose generator is given directly: ContinuousTimeMarkovChain(states, Q) represents a continuous-time chain on states with generator matrix Q (rows sum to zero, non-negative off-diagonals), and generator(Z) is just Q. (ContinuousTimeMarkovChain(Q), without states, uses the indices 1:size(Q, 1).)\n\nusing InfinitesimalGenerators\n\nstates = [0.5, 1.5]\nQ = [-0.1 0.1; 0.2 -0.2]\nZ = ContinuousTimeMarkovChain(states, Q)\ngenerator(Z)","title":"Finite-state continuous-time Markov chains"},{"category":"section","location":"univariate/#Diffusions","page":"Univariate processes","text":"DiffusionProcess(x, μx, σx) represents dx_t = mu(x_t)dt + sigma(x_t)dZ_t, given a strictly increasing grid x (possibly non-uniform) and the drift and volatility evaluated on it. Discretizing turns the diffusion into exactly the object above — a finite-state chain jumping between neighboring grid points. The drift is discretized by upwinding — forward differences where it is positive, backward where it is negative — and the boundaries are reflecting, so the discretized operator is always a valid generator matrix (rows sum to zero, non-negative off-diagonals):\n\nx = range(-1, 1, length = 100)\nX = DiffusionProcess(x, -0.03 .* x, 0.01 .* ones(100))\n@assert ndims(X) == 1 # hide\ngenerator(X)\n\nIn the finite-difference literature, a discretization with non-negative off-diagonal weights is called a monotone scheme — the property that guarantees convergence to the right (viscosity) solution of HJB equations, and the reason upwinding is the standard discretization there (see EconPDEs' discussion of upwinding). \"Monotone scheme\" and \"valid Markov generator\" are the same condition seen from two sides, which is why solutions move between the two packages exactly.","title":"Diffusions"},{"category":"section","location":"univariate/#Convenience-constructors","page":"Univariate processes","text":"The two workhorse processes come with constructors — OrnsteinUhlenbeck and CoxIngersollRoss — that choose the grid automatically:\n\nX = OrnsteinUhlenbeck(; xbar = 0.0, κ = 0.03, σ = 0.01)   # dx = -κ (x - xbar) dt + σ dZ\nXcir = CoxIngersollRoss(; xbar = 0.1, κ = 0.1, σ = 0.07)  # dx = -κ (x - xbar) dt + σ √x dZ\nnothing # hide\n\nBy default the grid spans the p and 1 - p quantiles of the stationary distribution with length points; pass length, xmin, xmax, or pow (grid-spacing power) to override. The default p = 1e-10 is deliberately extreme: reflecting boundaries distort solutions near the edges of the grid (see the expectations tutorial), and a wide grid pushes that distortion where the process never goes — which matters especially when computing tail indices.\n\nTo combine these building blocks — independent products, regime switching, correlated states — see Multivariate processes.\n\n@assert abs(sum(stationary_distribution(Z)) - 1) <= 1e-12 # hide\nnothing # hide","title":"Convenience constructors"},{"category":"section","location":"operators/#Operators","page":"Operators","text":"Once a process is represented by its generator matrix, everything else is linear algebra. This page lists the operators that apply to any ContinuousTimeMarkovProcess{N} — univariate or multivariate — with pointers to the tutorials that derive each one by hand.\n\nThroughout, take a simple process:\n\nusing InfinitesimalGenerators\n\nX = OrnsteinUhlenbeck(; xbar = 1.0, κ = 0.1, σ = 0.05)\nxs = only(state_space(X))\nnothing # hide","title":"Operators"},{"category":"section","location":"operators/#The-generator-itself","page":"Operators","text":"generator(X) returns the generator (transition-rate) matrix mathbbA of the discretized process — the operator f mapsto lim_t downarrow 0 left(Ef(x_t) mid x_0 = x - f(x)right)t acting on the flattened state space. Rows sum to zero and off-diagonals are non-negative, so the matrix can be used directly for anything the package does not provide: exponentials for transition probabilities, resolvents for present values, transposes for densities. Conversely, check_generator(𝔸) checks a matrix you built by hand (rows sum to zero, non-negative off-diagonals) before using it with the package, warning with the size of any violation.\n\n𝔸 = generator(X)","title":"The generator itself"},{"category":"section","location":"operators/#Stationary-distributions","page":"Operators","text":"stationary_distribution(X) solves the Kolmogorov forward equation mathbbA g = 0 and returns the probability mass at each grid point, shaped like size(X); to get a density, divide by the cell widths. See the distribution dynamics tutorial for the computation by hand:\n\ng = stationary_distribution(X)\nsum(g .* xs)   # ≈ xbar\n\nThe keyword form stationary_distribution(X; δ = δ, rebirth = ψ) computes the stationary distribution when agents die at rate delta and are reborn with distribution psi — the resolvent (delta I - mathbbA)^-1 delta psi — the relevant object in perpetual-youth and firm-entry models:\n\nψ = zeros(length(xs)); ψ[1] = 1.0    # everyone reborn at the bottom\ngδ = stationary_distribution(X; δ = 0.05, rebirth = ψ)\nsum(gδ .* xs)","title":"Stationary distributions"},{"category":"section","location":"operators/#Expectations:-Feynman–Kac","page":"Operators","text":"feynman_kac(X, ts; f, ψ, v, direction) computes conditional expectations of the general form\n\nEleftint_0^T e^-int_0^t v(x_s) ds f(x_t)  dt + e^-int_0^T v(x_s) ds psi(x_T) Big x_0right\n\nby implicit Euler time stepping — forecasts (ψ), flow payoffs and present values (f), state-dependent discounting or hazard rates (v). See the expectations tutorial for the computation by hand and the exact semantics of each keyword:\n\nu = feynman_kac(X, 0:0.1:10; ψ = collect(xs))   # E[x_T | x_t], one column per date\nmaximum(abs, u[:, 1] - (1.0 .+ exp(-0.1 * 10.0) .* (xs .- 1.0)))","title":"Expectations: Feynman–Kac"},{"category":"section","location":"operators/#Additive-functionals:-long-run-CGFs-and-tail-indices","page":"Operators","text":"An AdditiveFunctional(X, μm, σm) represents a cumulative quantity dm_t = mu_m(x_t) dt + sigma_m(x_t) dZ^m_t driven by the state — think m = log w for a size w growing at a state-dependent rate; X can be any process in the package. cgf(m, ξ) returns the long-run scaled cumulant generating function Lambda(xi) = lim_ttoinfty log Ee^xi m_tt — the principal eigenvalue of the tilted generator tilted_generator(m, ξ) — and tail_index(m; δ) returns the Pareto exponent of the stationary distribution of e^m under death rate delta. See the tail indices tutorial:\n\nm = AdditiveFunctional(X, collect(xs .- 1.0), 0.1 .* ones(length(xs)))\ntail_index(m; δ = 0.05)","title":"Additive functionals: long-run CGFs and tail indices"},{"category":"section","location":"operators/#Finite-differences","page":"Operators","text":"The finite-difference operators the package is built on are also exported, as lazy arrays whose entries are computed on demand. In one dimension, the grid is a vector and the function values a vector of the same length; in multiple dimensions, the grid is a tuple of axis vectors such as (xs, ys), dimensions are selected by number, and the function values are an array of matching size. The direction keyword accepts :forward/:backward and the EconPDEs-style synonyms :up/:down interchangeably:\n\nf = sin.(xs)\nFirstDerivative(xs, f; direction = :forward, bc = (0, 0))\nFirstDerivative(xs, f; direction = :backward, bc = (0, 0))\nSecondDerivative(xs, f; bc = (0, 0))\n\nys = range(0, 2, length = 40)\nF = [x * y for x in xs, y in ys]\ngrid = (xs, ys)\nFirstDerivative(grid, F, 1; direction = :forward)\nSecondDerivative(grid, F, 1, 1)\nSecondDerivative(grid, F, 1, 2; direction = :up)\nnothing # hide\n\nThe argument bc of FirstDerivative and SecondDerivative is the value of the first derivative at each limit of the grid. It defaults to zero, the right condition for reflecting boundaries — and the hook through which HJB boundary conditions like borrowing constraints enter (see the HJB tutorial). The convention is deliberately the same as the bc keyword of EconPDEs.jl's pdesolve (the outward first derivative at each boundary), so boundary conditions carry over unchanged between the two packages.\n\n@assert abs(sum(g) - 1) <= 1e-10 # hide\n@assert abs(sum(gδ) - 1) <= 1e-10 # hide\n@assert maximum(abs, u[:, 1] - (1.0 .+ exp(-0.1 * 10.0) .* (xs .- 1.0))) <= 1e-2 # hide\nnothing # hide","title":"Finite differences"},{"category":"section","location":"distributions/#Distribution-dynamics:-the-Kolmogorov-forward-equation","page":"Distribution dynamics","text":"The previous tutorial computed expectations, which march backward in time. Cross-sectional distributions march forward: given today's distribution of income, wealth, or firm size across agents, where is the distribution next year, and where does it settle in the long run? This tutorial computes both by hand from the generator matrix, and then introduces the helper stationary_distribution.\n\nTake the same Ornstein–Uhlenbeck process as before — now interpreted as the log productivity of a cross-section of firms:\n\nusing InfinitesimalGenerators, LinearAlgebra\n\nκ, σ = 0.1, 0.05\nX = OrnsteinUhlenbeck(; xbar = 0.0, κ = κ, σ = σ)\nxs = only(state_space(X))\n𝔸 = generator(X)","title":"Distribution dynamics: the Kolmogorov forward equation"},{"category":"section","location":"distributions/#Evolving-a-distribution-by-hand","page":"Distribution dynamics","text":"If expectations satisfy the backward equation partial_t u = mathbbA u, densities satisfy its adjoint, the Kolmogorov forward (Fokker–Planck) equation:\n\npartial_t g = mathbbA g\n\nThe transpose is not incidental: Ef(x_t) = g f for any test function, so whatever operator advances expectations backward, its transpose advances the distribution forward. On the discretized state space, g is the vector of probability masses at each grid point, and the forward equation is again a linear ODE solved with implicit Euler steps:\n\nfunction evolve(𝔸, g0, T; dt = 0.1)\n    B = factorize(I - dt * copy(𝔸'))\n    g = copy(g0)\n    for _ in 1:round(Int, T / dt)\n        g = B \\ g\n    end\n    return g\nend\nnothing # hide\n\nStart every firm at the same productivity — a point mass one standard deviation above the mean — and watch the cross-section spread out and recenter:\n\nusing Plots\n\ng0 = zeros(length(xs))\ng0[findmin(abs.(xs .- σ / sqrt(2κ)))[2]] = 1.0\n\nΔx = step(xs)\nplot(xlabel = \"log productivity x\", ylabel = \"cross-sectional density\")\nfor T in (1, 5, 20, 100)\n    plot!(xs, evolve(𝔸, g0, T) ./ Δx; label = \"t = $T\")\nend\ncurrent()\n\nThe division by Δx converts masses to a density for plotting: g sums to one without grid weights, so the density at a grid point is the mass divided by the cell width. On this uniform grid the two differ only by a constant factor; on a non-uniform grid (as in the HJB tutorial) the distinction matters, since raw masses would trace the grid spacing rather than the shape of the distribution.\n\nThe discretized process is an honest Markov chain — rows of mathbbA sum to zero and off-diagonals are non-negative — so the masses stay non-negative and sum to one at every date, with no renormalization needed:\n\nsum(evolve(𝔸, g0, 100))\n\nFor the Ornstein–Uhlenbeck process the transition distribution is known in closed form — Gaussian with mean e^-kappa t x_0 and variance fracsigma^22kappa(1 - e^-2kappa t) — which checks the discretization at, say, t = 5:\n\nt, x0 = 5.0, xs[findmax(g0)[2]]\ngt = evolve(𝔸, g0, t)\nmean_t = sum(gt .* xs)\nvar_t = sum(gt .* xs .^ 2) - mean_t^2\n(; mean_t, closed_mean = exp(-κ * t) * x0,\n   var_t, closed_var = σ^2 / (2κ) * (1 - exp(-2κ * t)))\n\nThe mean is essentially exact: the upwind scheme discretizes the drift kappa(bar x - x) without bias. The variance is a few percent too high — upwinding adds a little numerical diffusion, the price paid for a discretization that is guaranteed to be a well-defined Markov chain (masses stay non-negative no matter how coarse the grid). Refining the grid shrinks the gap.","title":"Evolving a distribution by hand"},{"category":"section","location":"distributions/#The-stationary-distribution-by-hand","page":"Distribution dynamics","text":"As t to infty the distribution converges to the stationary distribution, the fixed point of the forward equation:\n\nmathbbA g = 0 qquad textstylesum_i g_i = 1\n\nThat is a linear system: g is the null vector of the transposed generator, normalized to sum to one. By hand, replace one (redundant) row of mathbbA with the normalization:\n\nB = Matrix(𝔸')\nB[end, :] .= 1.0\ng∞ = B \\ [zeros(length(xs) - 1); 1.0]\nnothing # hide\n\nFor the Ornstein–Uhlenbeck process this should be the N(0 sigma^22kappa) density. The maximum gap, relative to the density's peak:\n\ns2∞ = σ^2 / (2κ)\nclosed∞ = @. exp(-xs^2 / (2s2∞)) / sqrt(2π * s2∞)\nmaximum(abs, g∞ ./ Δx - closed∞) / maximum(closed∞)\n\nThe same couple of percent as the variance check above — upwinding's numerical diffusion — and it halves with each doubling of the grid.","title":"The stationary distribution by hand"},{"category":"section","location":"distributions/#...-and-with-the-helper","page":"Distribution dynamics","text":"stationary_distribution performs this computation for any process in the package (using a robust eigenvalue method rather than the row-replacement trick, and reshaping the result to the shape of the state space for multivariate processes):\n\ng = stationary_distribution(X)\nmaximum(abs, g - g∞)\n\nTwo variations are worth knowing:\n\nConvergence check. The evolved distribution approaches the stationary one at the speed of mean reversion: maximum(abs, evolve(𝔸, g0, 100) - g) is of the order of the autocorrelation e^-kappa cdot 100 approx 5 times 10^-5, and by t = 300 the two agree to machine precision.\nDeath and rebirth. stationary_distribution(X; δ = δ, rebirth = ψ) computes the stationary distribution when agents die at rate delta and are reborn with distribution psi — the resolvent (delta I - mathbbA)^-1 delta psi — which is the relevant object in perpetual-youth and firm-entry models.\n\n@assert abs(sum(evolve(𝔸, g0, 100)) - 1) <= 1e-10 # hide\n@assert maximum(abs, g - g∞) <= 1e-10 # hide\n@assert abs(mean_t - exp(-κ * t) * x0) <= 1e-3 # hide\n@assert abs(var_t / (σ^2 / (2κ) * (1 - exp(-2κ * t))) - 1) <= 0.1 # hide\n@assert maximum(abs, evolve(𝔸, g0, 100) - g) <= 1e-5 # hide\n@assert maximum(abs, evolve(𝔸, g0, 300) - g) <= 1e-12 # hide\nnothing # hide","title":"... and with the helper"},{"category":"section","location":"expectations/#Expected-values:-the-Kolmogorov-backward-equation","page":"Expected values (Feynman–Kac)","text":"Many objects in economics are conditional expectations over the path of a Markov process: forecasts, present values, survival probabilities, option values. This tutorial computes them by hand from the generator matrix — each is a few lines of linear algebra — and then introduces feynman_kac, the helper function that packages the computation.\n\nThroughout, take a cash flow y_t that follows an Ornstein–Uhlenbeck process\n\ndy_t = kappa (bar y - y_t)  dt + sigma  dZ_t\n\na natural model of a mean-reverting dividend or income stream. The convenience constructor OrnsteinUhlenbeck chooses a grid spanning the stationary distribution and returns the discretized process:\n\nusing InfinitesimalGenerators, LinearAlgebra\n\nκ, σ, ȳ = 0.1, 0.05, 1.0\nY = OrnsteinUhlenbeck(; xbar = ȳ, κ = κ, σ = σ)\nys = only(state_space(Y))\n𝔸 = generator(Y)","title":"Expected values: the Kolmogorov backward equation"},{"category":"section","location":"expectations/#Forecasts-by-hand","page":"Expected values (Feynman–Kac)","text":"Fix a horizon T and consider the forecast u(y t) = Epsi(y_T) mid y_t = y. It solves the Kolmogorov backward equation\n\n0 = partial_t u + mathbbA u qquad u(cdot T) = psi\n\nwhere, on the grid, mathbbA is the matrix representation of the infinitesimal operator (mathbbAf)(y_i) = lim_Delta t downarrow 0 left(Ef(y_t+Delta t) mid y_t = y_i - f(y_i)right)  Delta t. After discretizing the state space, this is a linear ODE in mathbbR^n, solved by marching backward from T with implicit Euler steps:\n\nu_t - dt = (I - dt  mathbbA)^-1 u_t\n\nIn code — factorize once, then one back-substitution per time step:\n\nfunction expectation(𝔸, ψ, T; dt = 0.01)\n    B = factorize(I - dt * 𝔸)\n    u = copy(ψ)\n    for _ in 1:round(Int, T / dt)\n        u = B \\ u\n    end\n    return u\nend\n\nu = expectation(𝔸, collect(ys), 10.0)   # ψ(y) = y: the conditional mean E[y_T | y_0]\nnothing # hide\n\nFor the Ornstein–Uhlenbeck process the conditional mean is known in closed form, Ey_T mid y_0 = bar y + e^-kappa T(y_0 - bar y), which pins down the accuracy of the discretization:\n\nmaximum(abs, u - (ȳ .+ exp(-κ * 10.0) .* (ys .- ȳ)))\n\nThe error has two sources — the O(dt) bias of implicit Euler and the reflecting boundaries of the grid — and both are small because the time step is short and the grid is wide relative to the stationary distribution.","title":"Forecasts by hand"},{"category":"section","location":"expectations/#...-and-with-the-helper","page":"Expected values (Feynman–Kac)","text":"feynman_kac runs exactly this backward march (reusing the factorization, as above) and returns the whole time path. The terminal condition is the keyword ψ; the result has one column per date in ts, and the first column is the expectation at horizon ts[end] - ts[1]:\n\nts = range(0, 10, step = 0.01)\nu2 = feynman_kac(Y, ts; ψ = collect(ys))\nmaximum(abs, u2[:, 1] - u)\n\nBecause psi is arbitrary, probabilities are the same computation — a probability is the expectation of an indicator. The probability that the cash flow is below its long-run mean in ten years:\n\nprob = feynman_kac(Y, ts; ψ = float.(ys .<= ȳ))[:, 1]\nextrema(prob)","title":"... and with the helper"},{"category":"section","location":"expectations/#Present-values-by-hand","page":"Expected values (Feynman–Kac)","text":"Now price a claim to the flow y_t discounted at rate r:\n\nP(y) = Eleftint_0^infty e^-rt y_t  dt Big y_0 = yright\n\nDifferentiating with respect to the starting date gives the stationary backward equation r P = y + mathbbA P — the continuous-time analogue of \"price equals dividend plus discounted expected price\". Discretized, it is a single linear solve in the resolvent of the generator:\n\nr = 0.05\nP = (r * I - 𝔸) \\ collect(ys)\nnothing # hide\n\nThe closed form P(y) = bar y  r + (y - bar y)(r + kappa) — mean-reverting cash flows are discounted at r + kappa, not r — again gives a check, and this time it also reveals the one systematic error of the discretization. The package always imposes reflecting boundaries, i.e. a zero derivative at the edges of the grid, while the true P has slope 1(r+kappa) everywhere. The result is a boundary layer: the error is visible at the very edge of the grid, dies out within a few standard deviations, and is negligible where the process actually spends time:\n\nclosedP = ȳ / r .+ (ys .- ȳ) ./ (r + κ)\nerr = abs.(P - closedP)\ninterior = abs.(ys .- ȳ) .<= 3 * σ / sqrt(2κ)   # within three sd of the stationary mean\n(edge = maximum(err), interior = maximum(err[interior]), average = sum(stationary_distribution(Y) .* err))\n\nThis is why the grids chosen by OrnsteinUhlenbeck and CoxIngersollRoss span far into the tails (by default, the 10^-10 quantiles of the stationary distribution): the boundary distortion then sits where the process essentially never goes.","title":"Present values by hand"},{"category":"section","location":"expectations/#...-and-with-the-helper-2","page":"Expected values (Feynman–Kac)","text":"feynman_kac computes the general finite-horizon version,\n\nu(y 0) = Eleftint_0^T e^-int_0^t v(y_s) ds f(y_t)  dt + e^-int_0^T v(y_s) ds psi(y_T) Big y_0 = yright\n\nwith a flow payoff f, a state-dependent (or time-varying) discount rate v, and a terminal payoff ψ. With f = ys, v = r, and a horizon long enough that e^-rT approx 0, it converges to the resolvent solve above:\n\nts = range(0, 400, step = 0.25)\nP2 = feynman_kac(Y, ts; f = collect(ys), v = r .* ones(length(ys)))\nmaximum(abs, P2[:, 1] - P)\n\nThe state-dependent discount v is what makes the helper more general than the one-line resolvent: it prices claims under a stochastic short rate, computes expected values with state-dependent hazard rates of death or default, and handles time-varying payoffs (pass f as a matrix with one column per date).\n\n@assert maximum(abs, u2[:, 1] - u) <= 1e-10 # hide\n@assert maximum(abs, u - (ȳ .+ exp(-κ * 10.0) .* (ys .- ȳ))) <= 1e-2 # hide\n@assert maximum(err[interior]) <= 1e-4 # hide\n@assert sum(stationary_distribution(Y) .* err) <= 1e-6 # hide\n@assert maximum(abs, P2[:, 1] - P) <= 1e-2 # hide\nnothing # hide","title":"... and with the helper"},{"category":"section","location":"tail_index/#Tail-indices:-power-laws-from-random-growth","page":"Tail indices","text":"Wealth, firm size, and city size all have Pareto upper tails. The canonical explanation is random growth: if log size grows by increments whose distribution does not depend on size, and units die (or reset) at some rate delta, the stationary distribution has a power-law tail\n\nP(w_t  w) sim w^-zeta\n\nThe exponent zeta is pinned down by a simple condition. Let m_t = log w_t be the cumulative log growth, and define the long-run scaled cumulant generating function\n\nLambda(xi) = lim_t to infty frac1t log Elefte^xi m_tright = lim_t to infty frac1t log Eleftw_t^xiright\n\nThen zeta solves Lambda(zeta) = delta: in the stationary distribution, the zeta-th moment explodes at exactly the rate at which death truncates it. When the growth rate depends on a persistent Markov state — heterogeneous returns, firm productivity, entrepreneurial skill — Lambda is no longer a simple quadratic, but it remains computable: it is the principal eigenvalue of a tilted generator matrix (Hansen and Scheinkman 2009; Beare and Toda 2022; Gouin-Bonenfant and Toda 2023). This tutorial computes it by hand, and then with the helpers cgf and tail_index.","title":"Tail indices: power laws from random growth"},{"category":"section","location":"tail_index/#Setup:-growth-with-a-persistent-state","page":"Tail indices","text":"Suppose an individual's wealth grows at a rate that follows a persistent Ornstein–Uhlenbeck state x_t (mean zero, so no growth on average), plus idiosyncratic noise:\n\nd log w_t = x_t  dt + nu  dZ^m_t qquad dx_t = -kappa x_t  dt + sigma_x  dZ_t\n\nusing InfinitesimalGenerators, LinearAlgebra\n\nκ, σx = 0.1, 0.009           # growth-rate state: persistence and volatility\nν = 0.1                      # idiosyncratic volatility of wealth growth\nδ = 0.05                     # death rate: expected lifespan 20 years\n\nX = OrnsteinUhlenbeck(; xbar = 0.0, κ = κ, σ = σx)\nxs = only(state_space(X))\nμm = collect(xs)             # drift of log wealth = the state itself\nσm = fill(ν, length(xs))\nnothing # hide\n\n(The grids built by OrnsteinUhlenbeck deliberately extend to the 10^-10 quantiles of the state's stationary distribution: the principal eigenvalue is sensitive to the far tails of the state space, so wide grids matter here more than anywhere else.)","title":"Setup: growth with a persistent state"},{"category":"section","location":"tail_index/#The-CGF-by-hand","page":"Tail indices","text":"For a diffusion, Ee^xi m_t f(x_t) obeys the same backward equation as an ordinary expectation, except with a tilted generator: multiplying by e^xi m_t adds the instantaneous CGF of the increment, xi mu_m(x) + tfrac12xi^2 sigma_m(x)^2, to the diagonal:\n\nmathbbA_xi = mathbbA + textDiagonalleft(xi mu_m + tfrac12 xi^2 sigma_m^2right)\n\nAs t grows, Ee^xi m_t approx e^Lambda(xi) t where Lambda(xi) is the dominant eigenvalue of mathbbA_xi — a Perron–Frobenius eigenvalue, real and simple, because mathbbA_xi has non-negative off-diagonals. By hand:\n\n𝔸 = generator(X)\nΛ(ξ) = maximum(real, eigvals(Matrix(𝔸 + Diagonal(ξ .* μm .+ 0.5 .* ξ .^ 2 .* σm .^ 2))))\nΛ(1.0)\n\nThe tail exponent solves Lambda(zeta) = delta — a one-dimensional root-finding problem on a convex function:\n\nusing Plots, Roots\n\nζ_hand = find_zero(ξ -> Λ(ξ) - δ, (0.1, 10.0))\n\nξs = range(0, 4, length = 50)\nplot(ξs, Λ.(ξs); label = \"Λ(ξ)\", xlabel = \"ξ\", legend = :topleft)\nhline!([δ]; label = \"death rate δ\", linestyle = :dash)\nscatter!([ζ_hand], [δ]; label = \"tail index ζ\")","title":"The CGF by hand"},{"category":"section","location":"tail_index/#...-and-with-the-helpers","page":"Tail indices","text":"AdditiveFunctional(X, μm, σm) represents the pair (state, cumulative growth). cgf(m, ξ) returns Lambda(xi), computed as the principal eigenvalue of the tilted generator tilted_generator(m, ξ) by inverse iteration rather than a full eigendecomposition (use cgf_eigenvector(m, ξ, :right) — or :left — when you also need the associated eigenvector):\n\nm = AdditiveFunctional(X, μm, σm)\nΛ1 = cgf(m, 1.0)\nΛ1 - Λ(1.0)\n\ntail_index(m; δ) performs the root-finding:\n\nζ = tail_index(m; δ = δ)\n(ζ, ζ_hand)","title":"... and with the helpers"},{"category":"section","location":"tail_index/#Persistence-fattens-the-tail","page":"Tail indices","text":"With constant growth dlog w = barmu  dt + barsigma  dZ, the CGF is the quadratic Lambda(xi) = barmu xi + tfrac12barsigma^2 xi^2 and the tail index has a closed form, available as the scalar method tail_index(μ, σ; δ) — where μ is the arithmetic growth rate of w itself, barmu + barsigma^22:\n\nζ_const = tail_index(0.0 + ν^2 / 2, ν; δ = δ)\n(ζ_const, tail_index(AdditiveFunctional(X, zeros(length(xs)), σm); δ = δ))\n\nNow compare: the persistent economy has the same average growth rate (zero) and the same idiosyncratic volatility as this constant benchmark, yet its tail is markedly fatter (a smaller zeta):\n\n(persistent = ζ, constant = ζ_const)\n\nThe reason is that a persistent growth state adds long-run variance: individuals who draw a high x keep growing fast for 1kappa approx 10 years, and it is precisely those lucky histories that populate the far tail. Quantitatively, the persistent component contributes 2  textVar(x)kappa = sigma_x^2kappa^2 to the long-run variance of m_tsqrtt, so the tail behaves roughly like a constant economy with total variance nu^2 + sigma_x^2kappa^2:\n\nσ2_longrun = ν^2 + σx^2 / κ^2    # long-run variance of m_t / √t\nζ_approx = tail_index(σ2_longrun / 2, sqrt(σ2_longrun); δ = δ)\n(exact = ζ, gaussian_approximation = ζ_approx)\n\nThe approximation is close but not exact — the exact Lambda is not quadratic, and its curvature beyond the second cumulant is part of what the eigenvalue computation captures. This matters in applications: as Gouin-Bonenfant and Toda (2023) emphasize, treating the tail exponent with a two-moment approximation can misstate tail inequality substantially when growth rates are persistent.","title":"Persistence fattens the tail"},{"category":"section","location":"tail_index/#Beyond-diffusions:-growth-driven-by-a-discrete-state","page":"Tail indices","text":"AdditiveFunctional accepts any process in the package, not just diffusions. The leading case is growth driven by a discrete type — the Markov multiplicative processes of Beare and Toda (2022) — for example units that alternate between a normal and a high-growth regime:\n\nZg = ContinuousTimeMarkovChain([0.0, 0.06], [-0.1 0.1; 0.5 -0.5])   # states = growth rates\nmz = AdditiveFunctional(Zg, [0.0, 0.06], [ν, ν])\nζz = tail_index(mz; δ = δ)\n\nThe computation is the same principal-eigenvalue problem, now on a 2 times 2 tilted matrix — small enough to verify by hand against the defining condition Lambda(zeta) = delta:\n\nΛz(ξ) = maximum(real, eigvals(Matrix(generator(Zg)) + Diagonal(ξ .* [0.0, 0.06] .+ 0.5 .* ξ .^ 2 .* ν^2)))\nΛz(ζz) - δ","title":"Beyond diffusions: growth driven by a discrete state"},{"category":"section","location":"tail_index/#References","page":"Tail indices","text":"Hansen, L. P., and J. A. Scheinkman (2009): Long-Term Risk: An Operator Approach, Econometrica — the principal-eigenvalue characterization of long-run expectations Ee^xi m_t.\nBeare, B., and A. A. Toda (2022): Determination of Pareto Exponents in Economic Models Driven by Markov Multiplicative Processes, Econometrica — Lambda(zeta) = delta as the general condition for Pareto exponents.\nGouin-Bonenfant, É., and A. A. Toda (2023): Pareto Extrapolation: An Analytical Framework for Studying Tail Inequality, Quantitative Economics — using the tail exponent to discipline heterogeneous-agent models.\n\n@assert abs(Λ1 - Λ(1.0)) <= 1e-8 # hide\n@assert abs(Λz(ζz) - δ) <= 1e-4 # hide\n@assert abs(ζ - ζ_hand) <= 1e-3 # hide\n@assert ζ < ζ_const # hide\n@assert abs(ζ_const - tail_index(AdditiveFunctionalDiffusion(X, zeros(length(xs)), σm); δ = δ)) <= 1e-3 # hide\nnothing # hide","title":"References"},{"category":"section","location":"hjb/#Solving-HJB-equations","page":"Solving HJB equations","text":"The previous tutorials took the Markov process as given. In economic models, the process is usually chosen: a household picks consumption, a firm picks investment, and the law of motion of the state depends on that policy. The value function then solves a nonlinear Hamilton–Jacobi–Bellman equation.\n\nThe workhorse method for these problems in economics — Achdou, Han, Lasry, Lions, and Moll (2022), \"Income and wealth distribution in macroeconomics: a continuous-time approach\" — never calls a nonlinear solver. Each iteration resolves the nonlinearity pointwise (a first-order condition plus an upwind choice) and then takes one linear implicit time step. The linear objects it needs — upwind derivatives, the generator matrix of the policy-implied process, its transpose for the distribution — are exactly what this package provides. This tutorial replicates the method, and ends with EconPDEs.jl, which packages a more robust version of the same iteration.","title":"Solving HJB equations"},{"category":"section","location":"hjb/#The-model","page":"Solving HJB equations","text":"A household saves in a riskless asset a at rate r and earns an income that switches between y_l and y_h as a two-state Poisson process with intensities lambda_lh lambda_hl. With CRRA utility and discount rate rho, the value functions v_l(a) v_h(a) solve the coupled HJB equations\n\nrho v_j(a) = max_c  u(c) + v_j(a) (y_j + r a - c) + lambda_jk (v_k(a) - v_j(a))\n\nsubject to the borrowing constraint a geq underlinea.\n\nusing InfinitesimalGenerators, LinearAlgebra\n\nyl, yh = 0.5, 1.5            # income states\nλlh, λhl = 0.2, 0.2          # switching intensities\nr, ρ, γ = 0.03, 0.04, 2.0    # interest rate, discount rate, risk aversion\namin, amax = -yl / r + 0.001, 50.0\nas = amin .+ range(0, (amax - amin)^(1 / 2), length = 200) .^ 2   # finer near the constraint\n\nZ = ContinuousTimeMarkovChain([yl, yh], [-λlh λlh; λhl -λhl])\nu(c) = c^(1 - γ) / (1 - γ)\nnothing # hide","title":"The model"},{"category":"section","location":"hjb/#The-implicit-scheme","page":"Solving HJB equations","text":"Discretize a on a grid and stack the value functions into v (one column per income state). The discretized HJB is then a system\n\nrho v = u(c(v)) + mathbbA(c(v))  v\n\nnonlinear only through the policy c(v). The key trick of the scheme is where the policy is evaluated: compute c_n = c(v_n) from the current guess, freeze it, and take one backward time step of the equation that is now linear in the new value v_n+1:\n\nleft(left(rho + tfrac1Deltaright) I - mathbbA(c_n)right) v_n+1 = u(c_n) + tfrac1Delta v_n\n\nEach iteration therefore has three steps, and only the last one is a solve — a linear one; no nonlinear solver appears anywhere:\n\nThe policy from the current guess. The first-order condition u(c) = v_j(a) pins down consumption, with the finite difference chosen by the upwind rule: the forward difference where the implied saving is positive, the backward difference where it is negative, and c = y_j + ra at a point where neither is consistent (the drift is zero there). The borrowing constraint never needs to be imposed explicitly: it enters through the boundary condition v_j(underlinea) = u(y_j + runderlinea) on the backward difference — the bc keyword of FirstDerivative.\nThe generator under that policy: assets drift at rate y + ra - c within each income state (a zero-volatility DiffusionProcess per state), and income switches according to Z — together, a SwitchingProcess.\nOne implicit time step: a single sparse linear solve.\n\nfunction solve_implicit(v; Δ = 1000.0, tol = 1e-8)\n    for iter in 1:1000\n        # 1. the policy implied by the current guess, chosen by upwinding\n        c = similar(v)\n        for (j, y) in enumerate((yl, yh))\n            bc = ((y + r * amin)^(-γ), 0)  # borrowing constraint at amin; reflecting at amax\n            va_up = FirstDerivative(as, view(v, :, j); direction = :forward, bc = bc)\n            va_down = FirstDerivative(as, view(v, :, j); direction = :backward, bc = bc)\n            for i in eachindex(as)\n                budget = y + r * as[i]\n                # clamp marginal values: the iteration can visit v with negative differences\n                c_up, c_down = max(va_up[i], 1e-10)^(-1 / γ), max(va_down[i], 1e-10)^(-1 / γ)\n                if budget - c_up >= 0        # positive drift: use the forward difference\n                    c[i, j] = c_up\n                elseif budget - c_down <= 0  # negative drift: use the backward difference\n                    c[i, j] = c_down\n                else                         # steady point: consume the budget\n                    c[i, j] = budget\n                end\n            end\n        end\n        # 2. the Markov process for (a, y) implied by this policy\n        μa = [y + r * a for a in as, y in (yl, yh)] .- c\n        X = SwitchingProcess(Z, [DiffusionProcess(as, μa[:, 1], zeros(length(as))),\n                                 DiffusionProcess(as, μa[:, 2], zeros(length(as)))])\n        # 3. one implicit time step: a linear solve in the new value\n        vnew = reshape(((ρ + 1 / Δ) * I - generator(X)) \\ vec(u.(c) .+ v ./ Δ), size(v))\n        if maximum(abs, vnew - v) < tol\n            println(\"converged in $iter iterations\")\n            return (; v = vnew, c, X)\n        end\n        v = vnew\n    end\n    error(\"no convergence\")\nend\n\nv0 = [u(y + r * a) / ρ for a in as, y in (yl, yh)]   # guess: consume the budget forever\n(; v, c, X) = solve_implicit(v0)\nnothing # hide\n\nAt a fixed point v_n+1 = v_n, the Delta terms cancel and the exact discretized HJB holds, with a policy consistent with the value function.\n\nThe function above is, almost line for line, the reference Matlab implementation huggett_partialeq.m from Ben Moll's code library — and the correspondence is exact: run with the same parameters and grid, the two produce the same value function and stationary distribution to within 10^-8.","title":"The implicit scheme"},{"category":"section","location":"hjb/#The-distribution,-for-free","page":"Solving HJB equations","text":"Notice what step 2 of the solver builds: not just a matrix of finite-difference coefficients, but a bona fide Markov process for the household's state (a y) — a SwitchingProcess over two zero-volatility DiffusionProcesses, the same objects as in the previous tutorials. The X returned by solve_implicit is that process evaluated at the solved policy: the equilibrium wealth process. The matrix that was just used to solve the HJB is, at the fixed point, the generator of the model's cross-sectional dynamics, so everything from the previous tutorials applies to it directly — in particular the stationary wealth distribution is one more linear solve:\n\nusing Plots\n\ng = stationary_distribution(X)\n\nΔa = (as[[2:end; end]] .- as[[1; 1:(end - 1)]]) ./ 2   # cell widths of the non-uniform grid\nidx = as .<= 10\nplot(as[idx], (g ./ Δa)[idx, :];\n    label = [\"low income\" \"high income\"], xlabel = \"assets a\", ylabel = \"stationary density\")\n\nAs everywhere in the package, g holds probability masses — sum(g) == 1 with no grid weights — so aggregates are unweighted dot products like sum(g .* c) below. Plotting is the one place masses are the wrong units: on a non-uniform grid, raw masses trace the grid spacing rather than the shape of the distribution, so the plot divides by the cell widths to convert to a density. (Moll's codes use the opposite convention: there g holds density values, normalized and aggregated with da weights.)\n\nLow-income households dissave toward the borrowing limit; high-income households accumulate. And because the same discretized generator prices the value function and transports the distribution, accounting identities hold to machine precision rather than up to discretization error — aggregate consumption equals aggregate income exactly:\n\nC = sum(g .* c)\nY = sum(g .* [y + r * a for a in as, y in (yl, yh)])\nC - Y","title":"The distribution, for free"},{"category":"section","location":"hjb/#EconPDEs.jl:-the-robust-version","page":"Solving HJB equations","text":"The scheme above buys linearity by evaluating the policy at the lagged guess: the step solves for v_n+1 under the policy implied by v_n. The alternative — the fully implicit step — evaluates the policy at the new guess:\n\nleft(left(rho + tfrac1Deltaright) I - mathbbA(c(v_n+1))right) v_n+1 = u(c(v_n+1)) + tfrac1Delta v_n\n\nThe unknown now appears inside the policy, so each time step is a genuine nonlinear system, solved with Newton's method. This is what EconPDEs.jl implements, and it is what you want once the equation is more nonlinear than this tutorial's:\n\nRobustness. With the lagged policy and a fixed Delta, nothing forces the update to make progress: in highly nonlinear models the policy and the value can chase each other, and the iteration oscillates or diverges unless Delta and the initial guess are hand-tuned. The fully implicit step always converges for Delta small enough, so adapting Delta — shrinking it when a Newton step fails, growing it when it succeeds — converges without tuning. This adaptive scheme is pseudo-transient continuation, a standard method for stiff nonlinear PDEs in fluid dynamics; Kelley and Keyes (1998) give formal convergence conditions.\nSpeed. As Delta to infty the step becomes a pure Newton solve of the stationary equation, with quadratic convergence near the solution — against the linear convergence of the lagged iteration.\nConvenience. The sparse Jacobian is assembled automatically from the grid stencil, so none of the matrices above have to be derived by hand — the same code path handles multiple value functions, algebraic equations, and two- or three-dimensional state spaces.\n\nYou write only the equation at a single grid point, with named upwind derivatives — note that the policy inside is a function of the same value bundle being solved for:\n\nusing EconPDEs\n\nfunction household(state, value)\n    (; a) = state\n    (; vl, vla_up, vla_down, vh, vha_up, vha_down) = value\n    function upwind(y, va_up, va_down)\n        budget = y + r * a\n        c_up, c_down = max(va_up, 1e-10)^(-1 / γ), max(va_down, 1e-10)^(-1 / γ)\n        if budget - c_up >= 0\n            (c_up, va_up)\n        elseif budget - c_down <= 0 && a > amin\n            (c_down, va_down)\n        else\n            (budget, budget^(-γ))\n        end\n    end\n    cl, vla = upwind(yl, vla_up, vla_down)\n    ch, vha = upwind(yh, vha_up, vha_down)\n    vlt = -(u(cl) + (yl + r * a - cl) * vla + λlh * (vh - vl) - ρ * vl)\n    vht = -(u(ch) + (yh + r * a - ch) * vha + λhl * (vl - vh) - ρ * vh)\n    return (; vlt, vht)\nend\n\nresult = pdesolve(household, (; a = as), (; vl = v0[:, 1], vh = v0[:, 2]); verbose = false)\nmaximum(abs, [result.zero[:vl] result.zero[:vh]] - v)\n\nThe two methods find the same fixed point, because they discretize the equation identically. For the full workflow in the other direction — solve with pdesolve, then hand the policy-implied process back to this package for distributions and expectations — see the InfinitesimalGenerators page of the EconPDEs documentation.\n\n@assert abs(C - Y) <= 1e-10 # hide\n@assert abs(sum(g) - 1) <= 1e-10 # hide\n@assert result.residual_norm <= 1e-6 # hide\n@assert maximum(abs, [result.zero[:vl] result.zero[:vh]] - v) <= 1e-4 # hide\nnothing # hide","title":"EconPDEs.jl: the robust version"},{"category":"section","location":"#InfinitesimalGenerators.jl","page":"Home","text":"InfinitesimalGenerators.jl computes objects associated with continuous-time Markov processes — stationary distributions, conditional expectations, tail indices — by discretizing the process into its infinitesimal generator matrix.\n\nIt is designed for continuous-time economic models. Once a model is solved — for instance an HJB equation solved with EconPDEs.jl — the equilibrium law of motion of the state is a known Markov process, and the questions that remain are linear in that process:\n\nStationary distributions (the Kolmogorov forward equation): the distribution of wealth, firm size, or any state in the long run — see Distribution dynamics.\nConditional expectations (the Kolmogorov backward equation): present values, forecasts, survival probabilities — see Expected values.\nPower-law tail indices of stationary distributions generated by random growth (wealth, firm size, city size) — see Tail indices.\n\nThe same generator matrices are also the building blocks of the finite-difference method used to solve HJB equations in the first place — see Solving HJB equations.","title":"InfinitesimalGenerators.jl"},{"category":"section","location":"#Installation","page":"Home","text":"using Pkg\nPkg.add(\"InfinitesimalGenerators\")","title":"Installation"},{"category":"section","location":"#The-generator-matrix","page":"Home","text":"Every tool in the package is built on one object. For a Markov process x_t, the infinitesimal generator is the operator\n\n(mathbbAf)(x) = lim_t downarrow 0 fracEf(x_t) mid x_0 = x - f(x)t\n\nOn a discretized state space with n points, mathbbA becomes an n times n matrix with non-negative off-diagonal entries and rows summing to zero — the generator (transition-rate) matrix of a continuous-time Markov chain. The package constructs this matrix for univariate processes (DiffusionProcess, with convenience constructors OrnsteinUhlenbeck and CoxIngersollRoss; ContinuousTimeMarkovChain) and multivariate ones (ProductProcess, SwitchingProcess, MultivariateDiffusionProcess), and the operators built on it — stationary_distribution, feynman_kac, cgf, tail_index — apply to all of them.\n\nAll built-in process types subtype ContinuousTimeMarkovProcess{N}, where N is the number of tensor-product state-space axes. A scalar diffusion or a finite-state chain has N == 1; ProductProcess(X, Z) has N == ndims(X) + ndims(Z). The state shape is size(X), and arrays are flattened in Julia's column-major order when applying generator(X).\n\nusing InfinitesimalGenerators\n\n# an Ornstein-Uhlenbeck process dx = -0.03 x dt + 0.01 dZ\nx = range(-1, 1, length = 100)\nX = DiffusionProcess(x, -0.03 .* x, 0.01 .* ones(100))\ngenerator(X)\n\nDrift terms are discretized by upwinding (forward differences where the drift is positive, backward where it is negative) and boundaries are reflecting, so the discretized operator is always a bona fide generator: probability is conserved and the associated Markov chain is well defined. This is the same monotone scheme used to solve HJB equations, which is why solutions and distributions computed with this package are exactly consistent with finite-difference HJB solutions.","title":"The generator matrix"},{"category":"section","location":"#Tutorials","page":"Home","text":"Each tutorial first shows how to compute the object by hand from the generator matrix — they are all a few lines of linear algebra — and then introduces the helper function that packages the computation:\n\nExpected values: the Kolmogorov backward equation and feynman_kac.\nDistribution dynamics: the Kolmogorov forward equation and stationary_distribution.\nSolving HJB equations: the implicit finite-difference method of Achdou, Han, Lasry, Lions, and Moll (2022) — and EconPDEs.jl as its robust, automated version.\nTail indices: principal eigenvalues of tilted generators, cgf, and tail_index.","title":"Tutorials"},{"category":"section","location":"#Related-packages","page":"Home","text":"EconPDEs.jl solves the nonlinear PDEs (HJB equations) whose solutions this package takes as inputs. The two packages are designed to be used together.\nSimpleDifferentialOperators.jl contains more general tools to define operators with different boundary conditions. In contrast, InfinitesimalGenerators always assumes reflecting boundaries.","title":"Related packages"},{"category":"section","location":"multivariate/#Multivariate-processes","page":"Multivariate processes","text":"Multivariate processes live on tensor-product state spaces. They subtype ContinuousTimeMarkovProcess{N}, where N is the number of state-space axes: size(X) is the shape of the grid (e.g. (100, 2) for a diffusion crossed with a two-state chain), and generator(X) acts on states flattened in column-major order, so stationary_distribution(X) and feynman_kac(X, ...) return arrays shaped like size(X).\n\nStart from the univariate building blocks:\n\nusing InfinitesimalGenerators\n\nX = OrnsteinUhlenbeck(; xbar = 0.0, κ = 0.03, σ = 0.01)\nZ = ContinuousTimeMarkovChain([0.5, 1.5], [-0.1 0.1; 0.2 -0.2])\nnothing # hide","title":"Multivariate processes"},{"category":"section","location":"multivariate/#Products-of-independent-processes","page":"Multivariate processes","text":"ProductProcess combines independent processes into their joint process; the generator is assembled from Kronecker sums, and the state space is the tensor product in argument order:\n\nY = ProductProcess(X, Z)\n(ndims(Y), size(Y))","title":"Products of independent processes"},{"category":"section","location":"multivariate/#Regime-switching","page":"Multivariate processes","text":"SwitchingProcess(Z, Xs) drops the independence: the dynamics depend on the autonomous modulating process Z. In the i-th state of Z, the process follows Xs[i]; all modulated processes must share the same grid. Z can be any process in the package — a chain gives regime switching, and a diffusion gives continuous modulation (equivalent to a MultivariateDiffusionProcess with independent innovations, built compositionally). This is the natural representation of a solved model with a discrete state — see the HJB tutorial, where wealth drifts at a policy-implied rate that switches with income:\n\nxs = only(state_space(X))\nXlow  = DiffusionProcess(xs, -0.03 .* xs .- 0.01, 0.01 .* ones(length(xs)))\nXhigh = DiffusionProcess(xs, -0.03 .* xs .+ 0.01, 0.01 .* ones(length(xs)))\nS = SwitchingProcess(Z, [Xlow, Xhigh])\nsize(S)","title":"Regime switching"},{"category":"section","location":"multivariate/#Correlated-diffusions","page":"Multivariate processes","text":"MultivariateDiffusionProcess handles correlated diffusions on a tensor-product grid, with drift, variance, and covariance terms already evaluated on it (scalars or arrays), for example after solving an HJB:\n\nxs = range(-1, 1, length = 50)\nys = range(0, 2, length = 40)\ngrid = (; x = xs, y = ys)\n\nμx = repeat(-0.1 .* xs, 1, length(ys))\nμy = repeat(reshape(1 .- ys, 1, :), length(xs), 1)\nσx = 0.2 .* ones(length(xs), length(ys))\nσy = 0.3 .* ones(length(xs), length(ys))\ncovxy = 0.01 .* ones(length(xs), length(ys))\n\nY = MultivariateDiffusionProcess(grid;\n    drift = (; x = μx, y = μy),\n    variance = (; x = σx .^ 2, y = σy .^ 2),\n    covariance = (; xy = covxy),\n)\n\ng = stationary_distribution(Y)   # an array with size(Y)\nsize(g)","title":"Correlated diffusions"},{"category":"section","location":"multivariate/#When-is-the-discretization-a-valid-generator?","page":"Multivariate processes","text":"generator(Y) checks that the discretized operator is a valid Markov generator: rows must sum to zero and off-diagonal entries must be non-negative transition rates. Own-drift and own-variance terms always satisfy this, but the directional cross-derivative stencil used for correlated states is monotone only under a grid-scaled diagonal-dominance condition. For two states x and y, a useful local rule of thumb is\n\nabs(covxy) <= variance.x * Δy / Δx\nabs(covxy) <= variance.y * Δx / Δy\n\nOn equally spaced grids this becomes abs(covxy) <= min(variance.x, variance.y), which is stronger than positive semidefiniteness of the covariance matrix. If generator(Y) throws a negative-off-diagonal error, the most common fix is to rescale or refine the grids so Δx / Δy is closer to sqrt(variance.x / variance.y) in the region where the covariance is large. Equivalently, transform states so their local volatilities are more balanced, or reduce the covariance. Use generator(Y; check = :warn) or check = false only when you intentionally want the raw finite-difference operator without a Markov-process interpretation.\n\n@assert abs(sum(g) - 1) <= 1e-8 # hide\nnothing # hide","title":"When is the discretization a valid generator?"}]
}
